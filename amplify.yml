version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Next.js 15 + Turbopack
    baseDirectory: .next/output/public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
